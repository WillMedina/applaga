package com.dapm.applaga

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class HistorialClienteActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_historial_cliente) // Asegúrate de tener el layout correcto aquí

        // Cualquier inicialización adicional necesaria para HistorialActivity
    }
}
